---

import Icon from '../ui/AppIcon.astro';
import type { HTMLAttributes } from 'astro/types';

type ButtonType = "button" | "submit" | "reset";

interface Props extends Omit<HTMLAttributes<'button'>, 'type'> {
  textContent: string;
  type?: ButtonType;
  icon?: string;
  isPrimary?: boolean;
}

const {textContent, type = "button", icon = null, isPrimary = false, class: className, ...rest } = Astro.props;

const rawUuid = crypto.randomUUID();
const elementId = `btn-id-${rawUuid}`;

---

<button 
    id={elementId} 
    type={type} 
    class:list={[className, { invert: isPrimary }]}
    {...rest} 
>
    <p class="body-lg"> {textContent}</p>
    {icon && (
        <span class="button__icon">
            <Icon name={icon}></Icon>
        </span>
    )}
</button>

<style lang="scss">
    @use '../../styles/variables.scss' as *;

    button {
        background-color: var(--primary-color);
        border: 1px solid var(--secondary-color);
        color: var(--secondary-color);

        display: inline-flex;
        justify-content: space-around;
        
        padding: 1rem 2rem;

        &:hover {
            background-color: var(--secondary-color);
            border: 1px solid var(--primary-color);
            color: var(--primary-color);
            opacity: 50%;
        }

        &.invert {  
            background-color: var(--secondary-color);
            border: 1px solid var(--primary-color);
            color: var(--primary-color);

            &:hover {
                background-color: var(--primary-color);
                border: 1px solid var(--secondary-color);
                color: var(--secondary-color);
            }
        }
    }
    
</style> 
<script>

</script>